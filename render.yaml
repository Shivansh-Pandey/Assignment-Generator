services:
  - type: web
    name: assignment-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 360 --workers 1 --worker-class sync
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    autoDeploy: true
